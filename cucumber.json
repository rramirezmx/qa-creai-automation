{
  "default": {
    "require": [
      "features/steps/*.ts",
      "features/support/*.ts"
    ],
    "requireModule": [
      "ts-node/register"
    ],
    "format": ["progress-bar", "html:reports/qa-creai-automation-report.html"],
    "paths": ["features/*.feature"]
  }
}